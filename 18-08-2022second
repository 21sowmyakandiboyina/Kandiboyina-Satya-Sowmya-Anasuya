#include<stdio.h>
int main()
{
	int n,a[10],i,sum=0,f=1;
	printf("enter number of elements");
	scanf("%d",&n);
	printf("enter elements into the array");
	for(i=0;i<n;i++)
	{
		scanf("%d",&a[i]);
		f=f*a[i];
	}
	printf("%d",f);
}
